generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Lead {
  id        String   @id @default(cuid())
  name      String
  email     String
  business  String
  message   String
  createdAt DateTime @default(now())
}
